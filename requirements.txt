# Runtime dependencies (mirror of [project.dependencies] in pyproject.toml).
# Prefer `pip install -e .` or `uv sync`; this file is for plain `pip install -r`.
django>=5.0,<5.2
djangorestframework>=3.15
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
pymongo>=4.7
redis>=5
aiokafka>=0.10
python-dotenv>=1.0
gunicorn>=22.0; sys_platform != "win32"
